Prompt-01-Design

Claude, I want to create a new application for my silicone Mac. The purpose of the application is to render markdown files. The rendering should emulate what Obsidian can render, including full markdown syntax as well as MathJax to render mathematical notation between signs.
The application should allow me to open multiple files in tabs. I should be able to set preferences in my Mac so that I can open it with a right click. 
Later, we will create a version of the program for iOS, but for now it just needs to run on OSX. 
Do not create the application yet. Just tell me how you would do it. Also, if possible, give me an estimate for the number of tokens required so I can estimate if you can complete the task within the session window.

Update the last digit of the version number with every set of changes.